<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="in_out_anmi" parent="@android:style/Theme.Dialog">
        <item name="android:activityOpenEnterAnimation">@anim/popshow_anim</item>
        <item name="android:activityOpenExitAnimation">@anim/pophidden_anim</item>
        <item name="android:activityCloseEnterAnimation">@anim/popshow_anim</item>
        <item name="android:activityCloseExitAnimation">@anim/pophidden_anim</item>

    </style>
</resources>
